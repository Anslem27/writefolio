import 'package:flutter/material.dart';

class WallComponentLikeButton extends StatelessWidget {
  final Function()? ontap;
  final bool isLiked;
  const WallComponentLikeButton({super.key, this.ontap, required this.isLiked});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
